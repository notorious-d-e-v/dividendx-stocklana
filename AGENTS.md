# Repository operating rules

- Astra owns architecture, product decisions, factual review, and final acceptance.
- When the `astra-sol-delegation` skill is available, use it for bounded implementation in GPT-5.6 Sol with fresh context, explicit effort, disjoint file ownership, and no recursive delegation.
- Verify claims against current evidence and working code. Plans describe intent; they do not prove implementation.
- Never present a fixture, local receipt, simulated balance, or UI state as a live issuer, wallet, market, or onchain integration.
- Preserve user edits, approved design, artwork, narration, evidence, and the `/rehearsal/` route unless the assigned work explicitly changes them.
- Use credentials only for an authorized integration; never print them, copy them into source, or expose their contents through fixtures, frontend bundles, screenshots, logs, or tests.
- Start with [`docs/artifact-map.md`](docs/artifact-map.md), [`planning/status.md`](planning/status.md), [`planning/plan.md`](planning/plan.md), and [`planning/phase-work-orders.md`](planning/phase-work-orders.md). New work follows [`spec/annual-series-accounting.md`](spec/annual-series-accounting.md), [`spec/annual-series-sdk.md`](spec/annual-series-sdk.md), [`spec/annual-series-tests.md`](spec/annual-series-tests.md) and [`spec/annual-product.md`](spec/annual-product.md). The older accounting/SDK contracts belong to the preserved single-event rehearsal. Use [`apps/web/qa/README.md`](apps/web/qa/README.md) for verification.
- The annual program/SDK and local wallet app are accepted in [program review](planning/program-review.md) and [wallet review](planning/wallet-review.md). Public devnet deployment and the Raydium transaction harness are now accepted in [AMM review](planning/amm-review.md). Preserve the running local app/runtime and approved previews. Qualified live issuer settlement remains incomplete; no real funds or mainnet are authorized.
- The user supplied an Ondo read-only key for server-side issuer research. A bounded authenticated GET checker may read the external credential file and query documented Ondo data endpoints; keep the key and raw authenticated responses outside Git and the browser. Do not restart the user's test app or promote observations into settlement attestations.
- The server-side observation readers are accepted under [`spec/issuer-readers-v1.md`](spec/issuer-readers-v1.md); use [`planning/issuer-reader-review.md`](planning/issuer-reader-review.md) for their proof boundaries. Preserve annual cutoff/maturity rules and keep public-network recombination distinct from local post-maturity redemption.
- The separate `/demos/` page is accepted in [guided review](planning/guided-demo-review.md), under [`spec/guided-demos-v1.md`](spec/guided-demos-v1.md). Keep Split/Redeem core intact. The guided runtime uses port 4181 and its own offline chain, two disposable test wallets, captured genuine Raydium bytecode and explicit synthetic annual settlement. Preserve the existing 4180 runtime. Lending and other DeFi demos remain later, subject to actual venue support.
- The user authorized Test USDC and supplied devnet faucet funding. [Test USDC acceptance](planning/usdc-demo-review.md) and [specification](spec/guided-usdc-v1.md) govern the current guided quote: exact Circle mint clone with explicit synthetic local funding, and a separate verified public mode using checked transfers. Preserve older generic-quote evidence and never confuse local balances with faucet funds.
- Issuer qualification follows [`spec/issuer-qualification-v1.md`](spec/issuer-qualification-v1.md). Its offline dossiers remain blocked and unsigned; no approval override, EventInput generation or settlement writer. Preserve private input/output handling and separate current configured-factor corroboration from historical event qualification.
- Captured issuer mint custody is accepted in [`planning/issuer-custody-review.md`](planning/issuer-custody-review.md) under [`spec/issuer-custody-conformance-v1.md`](spec/issuer-custody-conformance-v1.md). The 15-mint local proof preserves raw mint bytes and uses synthetic holder balances/events; it does not enable live custody or settlement. Keep snapshots immutable and source/ELF hashes pinned.
- The latest user direction defers Streamflow, Jupiter Lock and all additional DeFi flows to noninteractive future entries on `/demos/`. Prioritize [issuer data qualification and settlement](planning/research/issuer-settlement-qualification-2026-09-17.md); do not interpret earlier integration discussions as a request to build more trading demos now.
- The 18 September hosting decision prioritizes a usable public site before the user's issuer outreach. Follow [hosting plan](planning/hosting-plan.md): real-calendar public devnet plus isolated online accelerated sandboxes, with no shortened devnet term or time override. Vercel-first hosting is a recommendation pending a Linux/provider probe. Preserve local defaults/processes; hosted test assets do not establish live issuer settlement.
- The user's next "sounds good, let's keep going" authorizes the planned Raydium test integration under [`spec/amm-integration-v1.md`](spec/amm-integration-v1.md), including dedicated devnet test funding/deployment and an isolated genuine-program local fallback. Use only worthless test assets and a dedicated signer; never the user's default wallet or mainnet. Preserve the running app/runtime. Public funding availability determines which network can actually be proved.
- For ordinary code changes run `npm test`, `npm run typecheck`, and `npm run build`. Run `npm run test:browser` when UI behavior, routing, browser configuration, or fixtures can affect end-to-end flows. Documentation-only changes need link and content checks rather than the code suite.
- For program or transaction-SDK changes also run the relevant `build:program`, `build:idl`, `test:transactions`, and `test:program` commands documented in the README. Use the isolated SDK `smoke:local` when signing, instruction assembly, ABI or transaction behavior changes. Keep generated IDL copies/vectors synchronized and record the tested ELF hash; arithmetic-only tests do not prove SBF execution.
- User authorization controls the work. These repository rules guide execution within that authorized scope and do not expand or revoke it.
- Keep changes narrow, preserve unrelated work, and report exact checks and remaining limitations.
