{
  "name": "@dividendx/devnet-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build:sdk": "npm --prefix ../transaction-sdk run build",
    "build": "npm run build:sdk --silent && tsc -p tsconfig.json",
    "typecheck": "npm run build:sdk --silent && tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "preflight": "npm run build --silent && node dist/src/cli.js preflight",
    "bootstrap": "npm run build --silent && node dist/src/cli.js bootstrap",
    "serve": "npm run build --silent && node dist/src/cli.js serve",
    "fund-holder": "npm run build --silent && node dist/src/cli.js fund-holder",
    "refresh-test-observations": "npm run build --silent && node dist/src/cli.js refresh-test-observations",
    "smoke-holder": "npm run build --silent && node dist/src/cli.js smoke-holder"
  },
  "dependencies": {
    "@dividendx/transaction-sdk": "file:../transaction-sdk",
    "@solana/spl-token": "0.4.14",
    "@solana/web3.js": "1.98.4"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
