# Historical validation evidence

Files whose names still begin with `DividendX` and end in `.validation.json`, `illustrated-v3/finalizer/historical/pre-rebrand-illustrated-v3.validation.json`, `illustrated-v1/astra-review.json`, and `illustrated-v1/baseline-sha256.json` are preserved evidence from before the DivX rebrand. Their hashes and paths do not validate the rewritten DivX artifacts.

The current DivX Stocklana decks have fresh validation receipts in `illustrated-v3/finalizer/DivX-Stocklana-2026.validation.json` and `illustrated-v4/finalizer/DivX-Stocklana-2026-v4.validation.json`.

The receipts in each current deck's `finalizer/historical/` directory validate the immediately preceding files from before the hostname migration. They remain raw historical evidence and do not validate the current `divx.payai.network` decks.
